######################################
###        Training parameters     ###
loss: 'celoss'
model: 'complex'
augment: false
batch_size: 1
max_epochs: 10
learning_rate: 0.001

checkpoint_path: '/home/pawel/projects/flooq/gsn-sn8/outputs/gsn/train_flood/2024-08-04_21-54-47/checkpoints/checkpoint.ckpt'

######################################
### Below are technical parameters ###

project_dir: ${oc.env:GSN_SN8_DIR}
train_csv: ${project_dir}/inputs/sn8_data_train.csv
val_csv: ${project_dir}/inputs/sn8_data_val.csv

hydra:
  run:
    dir: ${project_dir}/outputs/gsn/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}

output_dir: ${hydra:runtime.output_dir}
checkpoints_dir: ${hydra:runtime.output_dir}/checkpoints
save_fig_n_images: 10

logger:
  neptune:
    project: 'gsn/flood-complex-model'
    n_images: 5
    tags:
      - model=${model}
      - loss=${loss}
      - augment=${augment}
      - lr=${learning_rate}
      - batch=${batch_size}